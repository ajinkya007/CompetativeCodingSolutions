#include<stdio.h>
int main() {
	int T;
	long long int D[10000], units;
	int i,j,x,n;
	scanf("%d", &T);
	for(x=0;x<T;x++) {
		scanf("%d", &n);
		for(i=0;i<n;i++) {
			scanf("%lld", &D[i]);
		}
		i = 0;
		units = 0;
		s}
	printf("%lld\n", units);
	}
}
